<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+: the system builds the splash from the theme; the
         launcher icon is masked into the splash icon container. -->
    <style name="Theme.MatchThree.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.MatchThree</item>
    </style>
</resources>
